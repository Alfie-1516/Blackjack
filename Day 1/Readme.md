### Home Work 1

### 1. Create the Boilerplate
- Set up the basic HTML document structure.
- Title the page **"Gaming Table"**.

### 2. Duplicate the Header and Footer
- Copy the header and footer structure from the **Landing Page** to maintain consistency.

### 3. Implement Proper Semantic HTML Tags
- Use a `<main>` tag to represent the gaming table.
- Inside `<main>`, create **sections** for different areas.

### 4. Define Sections for the Game Components
#### Player's Cards Section
- Wrap in a `<section>` tag.
- Include an `<h2>` titled **"Player's Cards"**.
- Add a list representing the cards dealt in order.

#### Dealer's Cards Section
- Wrap in a `<section>` tag.
- Include an `<h2>` titled **"Dealer's Cards"**.
- Add a list representing the dealer's cards.

### 5. Add a Description List for Game Terms
- Create a `<section>` to contain a **description list (`<dl>`)**.
- List the following terms with their definitions:
  - **Hit**: (Define the action)
  - **Stand**: (Define the action)
  - **Split**: (Define the action)

### 6. Display Player's Options
- Add an `<h3>` with the text **"Player’s Options"** to indicate where options will be placed.

---

